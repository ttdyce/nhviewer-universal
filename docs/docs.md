# Code Documentation

### `android`

The `android` directory is for the Android flutter build. For the most part, assets and pages are designed in `android/src/main`. See more documentation at `android/src/main/README.md`.

### `lib`

The `lib` directory contains the program logic, including the API caller. See more documentation at `lib/README.md`.